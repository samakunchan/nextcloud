# Possible que ça ne sert plus à rien, mais je laisse la parce que j'ai oublier pourquoi je l'avais mis.
# Possible qu'il a été utile quand mes recherches de debug.
sudo docker exec -u www-data -it nextcloud php occ config:system:set trusted_domains 1 --value=nextcloudsama.devpapangue.com
